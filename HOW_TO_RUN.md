# دليل تشغيل مشروع imaginAI

هذا الملف يشرح لك الخطوات البسيطة لتشغيل الموقع بنفسك في أي وقت تريده، دون الحاجة للرجوع إليّ.

المشروع يتكون من قسمين يجب تشغيلهما معاً للعمل بشكل صحيح:
1. **الخوادم الخلفية (Backend)**: لتشغيل الذكاء الاصطناعي ومعالجة الفيديو.
2. **واجهة المستخدم (Frontend)**: لتشغيل الموقع الذي تراه في المتصفح.

---

## 🛠️ الخطوة الأولى: تشغيل الـ Backend

1. افتح موجه الأوامر (Command Prompt) أو PowerShell.
2. انتقل إلى مجلد `backend` داخل المشروع:
   ```cmd
   cd "C:\Users\hp hay\OneDrive\سطح المكتب\ImaginAI\backend"
   ```
3. قم بتفعيل البيئة الافتراضية للغة Python:
   ```cmd
   .\venv\Scripts\activate
   ```
   *(ستلاحظ ظهور كلمة `(venv)` في بداية السطر في موجه الأوامر مما يعني أنها تفعلت بنجاح).*
4. الآن، قم بتشغيل الخادم الخاص بالـ API:
   ```cmd
   python -m uvicorn app.main:app --port 8000
   ```
5. اتركه يعمل ولا تغلق هذه النافذة.

---

## 💻 الخطوة الثانية: تشغيل الـ Frontend (الموقع)

1. افتح نافذة موجه أوامر (Terminal) ثانية **جديدة** (لا تغلق النافذة الأولى).
2. انتقل إلى مجلد `frontend` داخل المشروع:
   ```cmd
   cd "C:\Users\hp hay\OneDrive\سطح المكتب\ImaginAI\frontend"
   ```
3. قم بتشغيل خادم واجهة المستخدم:
   ```cmd
   npm run dev
   ```
4. انتظر قليلاً حتى تظهر رسالة تفيد بأن السيرفر يعمل (مثلاً `Ready in ...` أو `Local: http://localhost:3000`).

---

## 🌐 الخطوة الثالثة: فتح الموقع

الآن بعد تشغيل كلا القسمين، افتح متصفح الإنترنت الخاص بك (مثل Google Chrome أو Edge) واذهب إلى الرابط التالي:
[http://localhost:3000](http://localhost:3000)

## 🛑 لإيقاف تشغيل الموقع:
عند الانتهاء، يمكنك إغلاق الموقع عن طريق الذهاب إلى نوافذ موجه الأوامر (Terminal) والضغط على `Ctrl + C` لإيقاف السيرفرات، ثم إغلاق النوافذ.
